import React from 'react';
import { Layout } from '../components';

export const SettingsPage: React.FC = () => {
  return (
    <Layout>
      <div className="page-container">
        <h2>設定ページ</h2>
        <p>アプリケーションの設定がここに表示されます。</p>
        <p>（開発中）</p>
      </div>
    </Layout>
  );
};
